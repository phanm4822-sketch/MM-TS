# Optional online DTW compilation and frozen visual extension build tools.
# Install requirements.txt first. Core offloading does not require these.
numba>=0.60
ninja>=1.11
# Headers used by the explicitly supported torch 2.9.1+cu126 visual kernel.
nvidia-cuda-cccl-cu12==12.6.77
